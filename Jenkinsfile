pipeline {
   agent any
      // environment {
      //    PATH='C:/Program Files/nodejs/'
      // }
   stages {
      stage ('Ionic run') {
         steps {
            sh 'ionic serve'
         }
      }
}
}

